/**
 * function signatures needs to match the code at
 * https://github.com/surveyjs/custom-widgets/tree/master/src
 *
 */
declare module "surveyjs-widgets" {
  export function select2tagbox(Survey: unknown, $: unknown): void;
  export function nouislider(Survey: unknown): void;
  export function sortablejs(Survey: unknown): void;
  export function bootstrapdatepicker(Survey: unknown, $: unknown): void;
}
