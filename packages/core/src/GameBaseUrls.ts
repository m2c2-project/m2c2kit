/**
 * Base URLs for game resources.
 */
export interface GameBaseUrls {
  /** Base URL for fonts and images. */
  assets: string;
  /** Base URL for CanvasKit wasm binary. */
  canvasKitWasm: string;
}
